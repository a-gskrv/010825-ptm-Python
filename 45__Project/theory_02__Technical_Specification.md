### 1. Необходимо реализовать поиск фильмов (MySQL)

1. По ключевому слову 
   * Поиск по названию фильма   
   * Результат ограничен 10 фильмами
   * По запросу пользователя отображаются следующие 10 результатов, или пока не закончатся


2. По жанру и диапазону годов выпуска
   * Перед вводом пользователю показываются:
     * Список всех жанров из таблицы жанров 
     * Минимальный и максимальный год выпуска фильмов в базе
   * Указывается нижняя и верхняя граница, например: от 2005 до 2012, или конкретный год
   * По запросу пользователя отображаются следующие 10 результатов, или пока не закончатся


### 2. Сохранение запросов (MongoDB)

Сохранять все поисковые запросы в MongoDB в коллекцию `final_project_<your_group>_<your_full_name>`.

```json
[
    {
        "timestamp": "2025-05-01T15:34:00",
        "search_type": "keyword",
        "params": {
            "keyword": "matrix"
        },
        "results_count": 3
   },
    {
        "timestamp": "2025-05-01T15:35:00",
        "search_type": "genre__years_range",
        "params": {
            "genre": "Action",
            "years_range": "2001-2010"
        },
        "results_count": 5
   }
]
```

### 3. Отображение статистики запросов

Выводить список из 5 популярных запросов на выбор пользователя:
* по частоте (самые популярные по убыванию частоты)
* по последним поискам (5 последних запросов по убыванию от новых к старым).
* Каждый запрос включает в себя:
  * тип запроса `search_type`: 
    * `keyword` or 
    * `genre__years_range`
  * параметры запроса `params`
  * количество строк в выборке `results_count`
  * время запроса `timestamp`


### 4. Технологии

1. Python
2. MySQL
3. MongoDB


### 5. Дополнительные требования (обязательные)

1. **Соблюдение правил PEP8**
   * Код оформлен согласно стандартам **PEP8** (отступы, пробелы, длина строк, имена переменных). 
   * Используются понятные имена переменных и функций.


2. **Читабельность кода**
   * Каждая функция выполняет одну понятную задачу, без лишней вложенности и дублирования.
   * Присутствуют комментарии там, где это необходимо (например, описание логики запроса).


3. **Разделение проекта на функции и модули**
   * Отсутствие дублирования кода
   * Повторяющиеся действия (например, подключение к базам, формат вывода, обработка ошибок) вынесены в отдельные функции.
   * Вместо копирования одного и того же кода в разных частях — используется повторное использование функций.
   * Используются переменные для повторяемых параметров.


4. **Надёжность** (устойчивость к ошибкам ввода пользователя, сбоям сети и т.д.)
   * Программа должна уметь обрабатывать ошибки пользователя:
     * Предупреждать пользователя о неверном вводе 
     * и предлагать сделать ввод заново
   * Информировать о системных сбоях и давать предложения по дальнейшей работе
   * Аварийное завершение программы на защите — худший из кошмаров!
   * Чтобы избежать этого, нужны тесты (как минимум, smoke-тесты)


5. **Проект необходимо разместить на GitHub**
   * ⚠️ Без чувствительных данных (пароли подключения и т.д.)❗


6. **Добавить описание проекта в файле `README.md`**
   * Как важный элемент портфолио для поиска работы


7. **Подготовить краткую презентацию проекта:**
   * (главным образом для себя, чтобы помнить, о чём говорить на презентации)
   * Что реализовано в проекте
   * Как именно это реализовано
   * С какими сложностями столкнулись
   * И, особенно (❗), как успешно вы их преодолели 😎


8. **Будьте готовы продемонстрировать:**
   * пример отклика программы на запросы
     * и по ключевым словам
     * и по году/жанру
   * пример пагинации
   * пример изменения статистики после очередного запроса
     * и по 5 самым популярным
     * и по 5 последним уникальным
   * все примеры ответов должны быть заранее протестированы 
   * а необходимая статистика заранее собрана


9. Этот проект — **итоговая работа сразу по двум курсам (Python и Database)**, поэтому:
   * Python-код обязательно должен содержать язык запросов SQL и MongoDB
   * В крайнем случае, (при использовании ORM) необходимо дублировать запрос 
     * на странице сайте (лучше)
     * или (в худшем случае) в окне терминала


### 6. По желанию можно добавить:

* Браузерную реализацию пользовательского интерфейса (Python-фреймворки: `Django`, `FastAPI`, `Flask`) 
* Реализовать дополнительные критерии поиска (по актёру, по рейтингу и т.д.)
* Выполнить логирование запросов с помощью декоратора.
* Продублировать логирование запросов (и добавить логирование ошибок) в лог-файлы `*.log`
* В общем, любое усложнение задачи приветствуется)